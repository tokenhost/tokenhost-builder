# Example environment values for the generated Next.js app.
# Copy to `.env.local` (do not commit) and fill in real values.

# Optional: Token Host auth / profile service integration (used by some pages/components).
# If unset, auth-related features should degrade gracefully.
NEXT_PUBLIC_GOOGLE_AUTH_DOMAIN=https://auth.tokenhost.com
NEXT_PUBLIC_BACKEND_URL=https://auth.tokenhost.com

# Optional: client-side uploads (DEV ONLY).
# WARNING: Anything prefixed with NEXT_PUBLIC_* is exposed to the browser.
NEXT_PUBLIC_WEB3_STORAGE_TOKEN=replace-me

# Deployment signing keys (NEVER commit real keys)
# Used by `tokenhost-web-template/contracts/deploy.js`
ANVIL_PRIVATE_KEY=replace-me
SEPOLIA_PRIVATE_KEY=replace-me
PRIVATE_KEY=replace-me

